# สถาปัตยกรรมและแผนการพัฒนา (อัปเดตล่าสุด)
วันที่: 2026-05-12

## 1. เป้าหมายของระบบ
สร้างระบบเก็บและวิเคราะห์แชท LINE OA ย้อนหลัง เพื่อค้นหาแชทที่ค้างตอบหรืองานที่ถูกลืม (เน้นงานสั่งผลิตที่ใช้เวลานาน) โดยใช้ AI (Hermes/Ollama) ในการวิเคราะห์และเรียนรู้บริบทของลูกค้าแต่ละคนอย่างต่อเนื่อง

## 2. สถาปัตยกรรมหลักแบบ 3 ประสาน (Two-Step Pipeline)

เราใช้จุดแข็งของอุปกรณ์ 3 ส่วนมาทำงานร่วมกัน:

1. **Supabase (Cloud) - "ด่านหน้า 24 ชม."**
   - ทำหน้าที่เปิดรับ Webhook จาก LINE ตลอดเวลา (ไม่ต้องใช้ Cloudflare Tunnel หรือเปิดพอร์ต NAS)
   - เก็บข้อมูลข้อความเบื้องต้นลงในฐานข้อมูล PostgreSQL (ตาราง `line_messages`)

2. **NAS (Synology DS218play) - "บรรณารักษ์ / Long-term Storage"**
   - NAS จะทำหน้าที่เบาที่สุดคือ ดึงข้อมูล (Sync) จาก Supabase แบบวันละครั้ง (หรือตามความถี่ที่ตั้งไว้) ผ่าน Task Scheduler
   - นำข้อมูลดิบมาจัดเก็บเป็นไฟล์ `.json` แยกตามปีและเดือน เพื่อเป็น Backup และให้ AI นำไปใช้งานต่อได้ง่าย

3. **Windows PC (Hermes AI) - "นักวิเคราะห์และสรุปผล"**
   - เมื่อเปิดคอมพิวเตอร์และรันสคริปต์ ตัว AI จะไปอ่านไฟล์ดิบ (JSON) เล่มใหม่ที่ NAS เตรียมไว้ให้
   - AI จะทำการแยกแยะว่าข้อความไหนเป็นของใคร และเขียนสรุปลงใน **แฟ้มประวัติรายบุคคล (.md)**
   - อ่านแฟ้มประวัติเพื่อดูบริบทงานที่ผ่านมา และออก **รายงานแจ้งเตือนแชทค้าง (Report)**

---

## 3. โครงสร้างข้อมูลและโฟลเดอร์บน NAS

```text
nas_drive/
├── raw_logs/
│   └── 2026/
│       └── 05/
│           ├── 11.json
│           └── 12.json        <-- NAS ดึงข้อมูลดิบจาก Supabase มาสร้างไฟล์นี้ทุกวัน
│
├── profiles/
│   ├── คุณสมชาย_ป้ายไฟ.md       <-- AI อ่านไฟล์ JSON แล้วมาอัปเดต/ต่อท้ายไฟล์นี้
│   └── คุณแอน_ไวนิล.md
│
└── reports/
    └── report-2026-05-12.md   <-- AI สรุปแชทค้างเพื่อแจ้งเตือนแอดมิน
```

---

## 4. แผนการดำเนินการ (Implementation Steps)

### Step 1: ฝั่ง Supabase [COMPLETED]
- รันไฟล์ `supabase-schema.sql` เพื่อสร้างตาราง `line_messages` (เก็บข้อความ) และ `system_heartbeat` (ป้องกันโปรเจกต์ Free Tier ดับ)
- สร้างและ Deploy Edge Function ชื่อ `line-webhook` เพื่อรับ Webhook จาก LINE และบันทึกลง Database (อัปเดตให้ดึง displayName จาก LINE API ได้)
- เชื่อมต่อ LINE Messaging API เข้ากับ URL ของ Edge Function ผ่านการ Verify บน LINE Developers Console สำเร็จ

### Step 2: ฝั่ง NAS (Sync Script) [COMPLETED]
- สร้างไฟล์สคริปต์ Node.js แบบ Native (`nas-sync.js`) 
- สคริปต์นี้จะอ่านข้อมูลจาก Supabase ย้อนหลัง (เฉพาะที่ยังไม่ได้ดึง) และเซฟเป็นไฟล์ JSON ตามโครงสร้าง `raw_logs/YYYY/MM/DD.json`
- นำสคริปต์นี้ไปตั้งเวลาใน Task Scheduler ของ NAS สำเร็จแล้ว (รันด้วย Exit Code 0)

### Step 3: ฝั่ง Windows (Smart AI Analyzer) [PENDING]
- สร้างสคริปต์ `analyze-chats.ts` สำหรับรันบน Windows
- สคริปต์นี้จะอ่านไฟล์ JSON จาก `raw_logs/`
- โยนข้อมูลให้ Hermes (Ollama) จัดหมวดหมู่ แยกเนื้อหา และอัปเดตไฟล์ใน `profiles/*.md`
- นำข้อมูลที่ได้มาตรวจสอบ Keyword สุ่มเสี่ยง (เช่น "โอนแล้ว", "ทำทันไหม") และสถานะการตอบของแอดมิน
- สร้างรายงานสรุปแชทค้างลงในโฟลเดอร์ `reports/`

---

## 5. ข้อจำกัดทางเทคนิค (Technical Limitations)
- **LINE Webhook ไม่สามารถดึงข้อความที่แอดมินพิมพ์ตอบจากแอป LINE OA ได้:** API จะส่งเฉพาะข้อความจากลูกค้ามาให้เท่านั้น การที่แอดมินไปตอบลูกค้าผ่าน LINE OA App จะไม่มี log ถูกสร้างขึ้นในระบบของเรา
- **วิธีแก้ปัญหา:** AI Analyzer ต้องใช้วิธีอนุมาน (Infer) จากข้อความล่าสุดของลูกค้า และระยะเวลาที่ทิ้งช่วง หากพบข้อความสุ่มเสี่ยงและเวลาผ่านไปนานเกินควร จะต้องแจ้งเตือนใน Report ทันที

---

## 6. กฎกติกาของ AI ในการวิเคราะห์ (Rules)
- **ไม่ฟันธง 100%:** ให้ใช้คำว่า "ควรตรวจสอบ", "น่าจะค้าง", "เสี่ยงว่างานจม" แทนคำว่า "แอดมินลืมแน่นอน" เนื่องจากเราอาจไม่เห็นการตอบกลับบางอย่างของแอดมิน
- **Keyword สำคัญที่ต้องตามงานนานกว่าปกติ (60-90 วัน):** งานผลิต, มัดจำ, ติดตั้ง, ป้ายไฟ, นัดรับ
- **คำที่ไม่นับว่าปิดเคส:** "รับทราบ", "เดี๋ยวเช็กให้", "ขอสอบถามก่อน", "รอสักครู่"
- **"ขอบคุณ" อย่างเดียวห้ามปิดเคส:** โดยเฉพาะถ้าบริบทก่อนหน้านั้นคือการโอนเงินหรือมัดจำ
